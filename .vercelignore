# The client talks to Supabase directly, so the serverless api/ folder is not
# deployed. (To use the serverless variant instead, un-ignore api/, set the
# SUPABASE_* runtime env vars, and point the client at it.)
api
electron
supabase
design_handoff
design-reference2
dist
release
node_modules
*.docx
gdd_extracted.txt
